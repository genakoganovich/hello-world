name: Hello World
on: [push]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a test  # ← Добавь name для шага (видно в логах)
        run: |
          echo "Hello, ${{ github.actor }}!"  # ${{ }} — переменная GitHub (твой логин)
          node --version || echo "Node not installed"